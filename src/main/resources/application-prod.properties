# Production profile (the Docker image starts with --spring.profiles.active=prod).
# Everything else comes from environment variables, see README.md and DEVELOPMENT.md.

# One JSON document per log line (Elastic Common Schema) for log collectors such as Loki, Elastic or CloudWatch.
logging.structured.format.console=ecs
logging.level.root=info
